export class TeamStats {
    constructor(name, played, win, loss, draw, points) {
      this.name = name;
      this.played = played;
      this.win = win;
      this.loss = loss;
      this.draw = draw;
      this.points = points;
    }
}